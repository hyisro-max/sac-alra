# Stage 8 — inline images

OpenWebUI v0.11.3 already renders descriptors whose type is `image` or MIME type
starts `image/`. SACAI preserves this path and publishes PNG previews with both.

Upload a PNG/JPEG and confirm it renders inside the user message. Complete a
PlanetIR job and confirm preview/histogram render inside the assistant response
and also appear in Files. TIFF remains a downloadable file, not a browser image.

